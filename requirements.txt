# TeleVault dependencies
# Pin major versions; update deliberately, not automatically.

# --- Userbot (Phase 1) ---
telethon>=1.36
python-dotenv>=1.0

# --- API server (Phase 2) ---
fastapi
uvicorn[standard]   # includes uvloop + httptools for production performance
pydantic>=2.0       # schemas use Pydantic v2 model_config and field syntax

# --- Storage (Phase 4: PostgreSQL migration, in progress - see CHANGELOG) ---
sqlalchemy>=2.0     # Core (not the ORM) - see db/ for why
psycopg[binary]>=3.1  # Postgres driver; [binary] avoids needing build tools locally
alembic>=1.13       # schema migrations, replacing the old hand-rolled db/migrations/ scripts